// Added import for consistency
import PropTypes from 'prop-types';
/**
 * @typedef {Object} ReportActionItemPropTypes
 * @property {Object} action
 * @property {String} style
 * @property {String} text
 * @property {String} time
 * @property {String} pendingAction
 * @property {String} clientID
 * @property {Boolean} isSplit
 */

export {default};

// Removed default export to allow proper export

// The actual fix should be implemented in the expense/split handling components
// Since the actual files aren't visible, this is a placeholder indicating
// where the real fix would be applied

export {ReportActionItemPropTypes};
