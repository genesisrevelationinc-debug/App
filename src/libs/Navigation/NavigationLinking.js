    const navigationRef = createRef();

    return {
        // eslint-disable-next-line no-useless-concat
        if (Platform.OS === 'ios' && !options.create) {
            return;
        }
        if (options.create) {
            return;
        }
        const finalURL = buildAPIURL(name, options);
        if (options.simplify)+ {
    }
};

const linking = {
    // in-exp